# All /api/* requests are proxied to the Go backend via nginx (http://api:8080).
# VITE_API_URL is intentionally empty — axios uses relative URLs so requests
# go through nginx on the same origin. Do NOT set this to a direct backend IP
# as it would bypass nginx and break CORS / auth middleware.
VITE_API_URL=
